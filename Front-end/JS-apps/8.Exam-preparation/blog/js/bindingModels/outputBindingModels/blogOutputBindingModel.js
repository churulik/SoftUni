var BlogOutputBindingModel = (function () {
    function BlogOutputBindingModel(title, text, author) {
        this.title = title;
        this.text = text;
        this.author = author;
    }

    return BlogOutputBindingModel;
})();
